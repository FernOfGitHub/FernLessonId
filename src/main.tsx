import React from 'react';
import ReactDOM from 'react-dom/client';
import FernIdentifier from './FernIdentifier';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FernIdentifier />
  </React.StrictMode>
);
